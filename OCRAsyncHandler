public with sharing class OCRAsyncHandler {
    @AuraEnabled
    public static void startOcrAsyncJob(String contentDocumentId) {
        // Enqueue the job to process OCR
        System.enqueueJob(new OCRProcessor(contentDocumentId));
    }
}
